{
  "name": "landing",
  "version": "1.0.0",
  "description": "app landing page",
  "dependencies": {
  },
  "devDependencies": {
    "hugo-bin": "^0.89.0"
  },
  "pnpm": {
    "overrides": {
      "http-cache-semantics": ">=4.1.1",
      "semver-regex": ">=3.1.4",
      "got": ">=11.8.5"
    }
  },
  "scripts": {
    "start": "./server.sh",
    "build": "hugo --cleanDestinationDir -s . -d ./build"
  }
}
